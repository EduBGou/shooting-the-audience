using Godot;
using System;

public partial class GlobalEnums : Node
{
    public enum EColor
    {
        Red, Green, Blue, Yellow
    }
    public enum EState
    {
        Idle, Sneaking, Hidded, Dead
    }
}
